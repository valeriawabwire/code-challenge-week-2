function title(){
let title ="The Quick Brown Fox"
console. log(str. split(""). join(""));
var newtitle= str. split(""). map(item => (item ===item. toUpperCase() ? item. toLowerCase() :
item. toUpperCase())). join("");
};
console. log(newtitle);



